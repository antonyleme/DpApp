import { StyleSheet } from 'react-native';

import { primary } from '../../theme/colors';

export default StyleSheet.create({
    wrapper: {
        marginLeft: 15,
        marginBottom: 20,
    },

    image: {
        resizeMode: 'cover',
        borderRadius: 10,
        marginRight: 10,
    }
})